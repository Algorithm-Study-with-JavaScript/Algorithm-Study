// 내 풀이: floor을 사용해 실수 아래 최대 정수 구함
function solution(num1, num2) {
    var answer = Math.floor(num1 / num2);
    return answer;
}

//다른 사람 풀이: trunc 사용해서 소수점을 자름
function solution(num1, num2) {
    return Math.trunc(num1 / num2);
}
